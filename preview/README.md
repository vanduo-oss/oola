# OOLA draft preview

Vue 3 + Vite + `@vanduo-oss/vd3` — hand-authored Structured Phi 24×24 smoke icons.

```bash
npm install && npm run dev
# or: npm run preview:dev
```

Icons: black on default primary; follow `--vd-color-primary` when customized.
